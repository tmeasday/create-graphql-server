test:
  pre:
    - cd test/output-app
    - yarn
    - npm start:
        background: true
    - sleep 60
  override:
    - npm test
    - npm run output-app-generation-test
    - npm run end-to-end-test
