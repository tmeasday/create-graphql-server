dependencies:
  override:
    - yarn

test:
  pre:
    - cd test/output-app
    - yarn
    - npm start:
        background: true
    - sleep 10
  override:
    - npm test
    - npm run output-app-generation-test
    - npm run end-to-end-test
